# DeepNeuralNetwork-Example
A python code example of Deep Neural Network (with two hidden layers) from scratch without using any library except numpy. to train on a simple data set of Fibonacci series.
